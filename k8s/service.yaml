apiVersion: v1
kind: Service
metadata:
  name: agentic-idp-service
  namespace: idp-system
  labels:
    app: agentic-idp
spec:
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: agentic-idp
  type: ClusterIP
